<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MojePosilki"
        tools:targetApi="31">
        <activity android:name=".MainActivity"
            android:exported="true">
            <!-- Make it the launch activity (entry point of your app) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddRecipeActivity">
        </activity>

        <activity
            android:name=".ViewMealsActivity">
        </activity>

        <activity
            android:name=".CalendarActivity">
        </activity>

        <activity
            android:name=".ModifyMealActivity">
        </activity>

        <activity
            android:name=".IngredientsCalendar">
        </activity>

        <activity
            android:name=".WeightAcitivity">
        </activity>

        <activity
            android:name=".AddWeightActivity">
        </activity>

        <activity
            android:name=".ExerciseActivity">
        </activity>

    </application>
</manifest>